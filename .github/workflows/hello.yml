name: Hello Action with Email

on: [push]  # Triggers on any push to any branch

jobs:
  test:
    runs-on: ubuntu-latest  # Free Linux runner (virtual machine)
    steps:
      - name: Checkout code
        uses: actions/checkout@v4  # Grabs your repo files

      - name: 23f2002819@ds.study.iitm.ac.in  # Your required step name
        run: echo "Hello from GitHub Actions! Email step complete."  # Simple command
